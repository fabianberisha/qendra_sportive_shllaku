import 'package:flutter/material.dart';

class TerminetScreen extends StatelessWidget {
  static const routeName = '/terminet_screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('TS')),
      body: Text('TScreen'),
    );
  }
}
